@{
    ViewData["Title"] = "Home Page";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary">Ahorra Ya - Panel de Control</h1>
        <p class="lead">Sistema de gestión de inventario y precios.</p>
        <hr class="w-25 mx-auto" />
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow border-0">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-check-circle"></i> Módulos Disponibles (MVP)</h5>
                </div>
                <div class="card-body">
                    <p>Actualmente, el **WebClient (MVC)** tiene operativos los siguientes catálogos con CRUD completo, búsqueda y ordenamiento:</p>
                    <div class="list-group">
                        <a href="/Brand" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                            <span><strong>Brands</strong> (Marcas)</span>
                            <span class="badge bg-primary rounded-pill">Activo</span>
                        </a>
                        <a href="/Categories" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                            <span><strong>Categories</strong> (Categorías)</span>
                            <span class="badge bg-primary rounded-pill">Activo</span>
                        </a>
                    </div>
                    <p class="text-muted mt-3 small">* El resto de las entidades están disponibles únicamente vía WebAPI por el momento.</p>
                </div>
            </div>

            <div class="card shadow border-0 mt-4">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0"><i class="fas fa-key"></i> Configuración de Acceso API</h5>
                </div>
                <div class="card-body bg-light">
                    <p class="mb-2">Para el funcionamiento de los módulos, se debe obtener el token en la WebAPI con las siguientes credenciales:</p>
                    <table class="table table-sm table-bordered bg-white">
                        <thead>
                            <tr class="table-secondary">
                                <th>Usuario</th>
                                <th>Password</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td><code>Admin</code></td>
                                <td><code>Adm1n!</code></td>
                            </tr>
                        </tbody>
                    </table>
                    <div class="alert alert-info py-2 mt-2">
                        <small><strong>Nota:</strong> El token generado debe colocarse en el campo correspondiente dentro de los controladores <code>BrandController</code> y <code>CategoryController</code> para habilitar las peticiones autorizadas.</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>